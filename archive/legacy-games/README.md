# Legacy Games Data

This folder stores the archived `src/data/games.js` file.

Sara's Lab now uses `src/data/labProjects.js` as the active source of truth for project cards and detail views. Keep this archive for reference only unless the old game-only data model is deliberately restored.
